apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-db
  namespace: todo-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-db
  template:
    metadata:
      labels:
        app: todo-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_DB
              value: "tasksdb"
          ports:
            - containerPort: 5432
