apiVersion: v1
kind: Service
metadata:
  name: todo-api-service
  namespace: todo-namespace
spec:
  type: NodePort
  selector:
    app: todo-api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
